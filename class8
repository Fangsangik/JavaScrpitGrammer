//함수 선언문
function func(num){
    if (10 > num){
        return 'Hello';
    }
}

//함수를 호출 
console.log(func(1)); 

//JS의 경우 return이 없으면 undefined를 반환 

function voidFunc(){
    console.log('void func');
}

function sum (num1 , num2) {
    return num1 + num2;
}

console.log(sum(1, 2));

function multiple (num1, num2){
    return num1 * num2;
}

console.log(multiple(10 , 10));

//매개 변수가 많아지면 순서를 지키기 어렵다. 
//argument를 사용해서 동적으로 해결 할 수 있다. 