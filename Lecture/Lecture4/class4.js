/**
 * 참 같은 값 
 * Truthy
 * 원시 값이든 객체 값이든 참으로 평가할 때
 * 
 * Boolean형으로 평가 될 수 있는 것들 -> truthy 
 * 식으로 평가되는 항목들은 모두 참으로 평가 
 * 다양한 JS 식들이 Boolean 형으로 평가 될때 사용 ss
 */

if (true){ //참으로 동작되는 것들 
}

console.log({} && "hello");

